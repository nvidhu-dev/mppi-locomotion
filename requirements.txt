# dm_control 1.0.41 needs mujoco>=3.8.1, but mujoco 3.9.x renamed an internal
# field (flex_bandwidth) that dm_control's indexer still expects, so cap at <3.9.
dm_control>=1.0.41
mujoco>=3.8.1,<3.9
numpy
imageio[ffmpeg]
# Fetches the Unitree Go2 model from MuJoCo Menagerie (for the `go2` domain).
robot_descriptions
